// Input file that is used with expand_template_instantiations.cc
// to process .inst.in files into .in files

// Scalar types
REAL_SCALARS := { float; double }

// Space dimensions
SPACE_DIMENSIONS := { 1; 2; 3 }

// Element degrees
ELEMENT_DEGREE := { 1; 2@_additional_degrees@ }

// Non-associative containers
CONTAINERS := { std::vector; std::set; std::list }

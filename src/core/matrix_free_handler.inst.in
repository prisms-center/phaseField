for ( dimension : SPACE_DIMENSIONS; number : REAL_SCALARS)
  {
    template class MatrixFreeHandler<dimension, number>;

    template class MatrixFreeContainer<dimension, number>;
  }
for ( dimension : SPACE_DIMENSIONS; degree : ELEMENT_DEGREE; number : REAL_SCALARS)
  {
    template void MatrixFreeContainer<dimension, number>::reinit<degree, 1>(
      const dealii::Mapping<dimension> &,
      const DofHandler<dimension> &,
      const ConstraintHandler<dimension, degree, number> &,
      const dealii::Quadrature<1> &);
  }

for ( dimension : SPACE_DIMENSIONS; degree : ELEMENT_DEGREE; number : REAL_SCALARS)
  {
    template class ElementVolume<dimension, degree, number>;

    template class ElementVolumeContainer<dimension, degree, number>;
  }
